# Tasks

## 1. Verificación técnica previa

- [ ] 1.1 Probar el mecanismo de vista previa (interstitial + metarefresh, ver design.md) contra el grupo real de WhatsApp con un link de prueba, y verificar que la tarjeta muestra las metatags genéricas y que un click humano llega al destino
- [x] 1.2 Revisar las rutas de detalle de incidente/requerimiento/mascota y confirmar si exigen `Usuario.verificado` además de `visibleVecinos`; documentar el hallazgo (condiciona la tarea 5.3) — **Hallazgo:** ninguna de las tres rutas (`src/app/api/incidentes/[id]`, `requerimientos/[id]`, `mascotas/[id]`) ni sus Server Components (`src/app/(dashboard)/.../[id]/page.tsx`) chequean `verificado`; tampoco lo hace `src/app/(dashboard)/layout.tsx` (solo exige sesión). `verificado` hoy es un estado visible en el panel de admin, no un gate de acceso. Único check real es `visibleVecinos` para `VECINO` en incidentes. Consecuencia: la tarea 5.3 se omite (condición no se cumple) — CA-05.2 se cumple de forma directa, sin pantalla intermedia de espera.
- [x] 1.3 Probar manualmente el flujo de login/registro completo con `callbackUrl` seteado (no solo `src/proxy.ts`) y confirmar si NextAuth redirige al destino original después de autenticarse — **Hallazgo (leído en código, confirma el riesgo sin esperar prueba manual):** `src/app/(auth)/login/page.tsx` ignora `callbackUrl` por completo — `handleSubmit` hace `router.push("/inicio")` fijo, sin leer `useSearchParams()`. El link "Registrate aquí" tampoco lo propaga a `/registro`. Esto confirma que el punto de fuga real está acá, no en `proxy.ts`. Se corrige en la tarea 5.1/5.2. También se confirma que **ya existe** un mensaje de "cuenta en revisión" tras registrarse (`registro/page.tsx`, diálogo de éxito) — cubre CA-05.4 sin trabajo nuevo.

## 2. Schema de Prisma

- [x] 2.1 Agregar modelos `PublicacionExterna`, `LinkRastreable`, `ClickLink`, enums `CanalExterno`/`TipoEventoPublicacion`/`EstadoPublicacion`/`ModoPublicacion`/`OrigenRegistro`, y campo `Usuario.origenRegistro` (default `DIRECTO`) — verificar con `prisma migrate dev` y `prisma generate` sin errores. Migración `20260922203157_whatsapp_bridge_fase1` aplicada, `prisma generate` OK.
- [x] 2.2 Correr el typecheck y la suite de tests existente para confirmar que la migración aditiva no rompe nada — `npx tsc --noEmit` sin errores. **Hallazgo:** el proyecto no tiene framework de tests configurado (sin vitest/jest, sin script `test` en `package.json`, sin archivos `*.test.*`). Las tareas siguientes que pedían "verificar con un test" se van a verificar con scripts puntuales o revisión manual en su lugar — no se instala un framework de testing nuevo, es un cambio de infraestructura no pedido y fuera del alcance de este change.

## 3. Link rastreable y redirección

- [x] 3.1 Generador de código corto no enumerable (`crypto.randomBytes`, no `Math.random`) — implementado en `src/lib/notificaciones-externas/links.ts` (mismo patrón que `src/lib/crecimiento/invitaciones.ts`); verificado con script puntual: 8 caracteres, sin colisiones ni secuencia
- [x] 3.2 Ruta `GET /r/[codigo]`: sirve HTML con metatags OG genéricas (o por `tipoEvento`, nunca con contenido del evento) + redirección del lado del cliente a `urlDestino` — implementado como `src/app/r/[codigo]/route.ts` (interstitial) + `src/app/r/[codigo]/ir/route.ts` (redirección real), ver decisión de design.md. Verificado con `curl` contra el dev server: la interstitial sirve las metatags genéricas y `/ir` responde 307 al destino real. **Falta la verificación manual contra WhatsApp real (tarea 1.1) — sigue pendiente, esto solo confirma el mecanismo local.**
- [x] 3.3 Registrar cada click en `ClickLink` (`tenerSesion`, `usuarioId` si resuelve, sin IP ni user-agent completo) e incrementar `LinkRastreable.clicks` — verificado con `curl`: click sin sesión quedó registrado con `tenerSesion: false`, `usuarioId: null`, y `clicks` se incrementó
- [x] 3.4 Aplicar `rateLimit()` existente (`src/lib/api/rateLimit.ts`) por IP sobre `/r/[codigo]` — aplicado en `/r/[codigo]/ir` (el endpoint que hace el trabajo real). Verificado con 35 requests seguidos: las primeras 30 responden 307, el resto 429
- [x] 3.5 Página de destino muestra un estado "contenido no disponible" cuando la ruta real devuelve 403/404, en vez de propagar el error crudo — **ya cubierto por código existente, sin cambios:** las tres páginas de detalle (`incidentes/[id]`, `requerimientos/[id]`, `mascotas/[id]`) llaman `notFound()` de `next/navigation`, que renderiza `src/app/not-found.tsx` (página propia, con marca) — no hay error crudo en ningún caso. `incidentes/[id]/page.tsx:46` ya hace exactamente el chequeo de `visibleVecinos` que esta tarea pedía verificar.

## 4. Acción de compartir

- [x] 4.1 Módulo de plantillas centralizado (`src/lib/notificaciones-externas/plantillas.ts`) para incidente/requerimiento/mascota, con nombre + inicial del apellido (o "Un vecino" si el usuario optó por no mostrarse) — verificado con script puntual (sin framework de tests, ver hallazgo de 2.2): ningún mensaje generado contiene descripción/lote/contacto/foto, y el opt-out oculta el apellido correctamente. **Hallazgo:** D1 pedía opt-out por usuario pero no había campo en el schema ni UI para configurarlo — se agregó `Usuario.ocultarNombreEnPublicacionExterna` (migración `whatsapp_bridge_opt_out_nombre`, default `false`); la plantilla ya lo respeta, pero **no hay pantalla para que el usuario lo active** — queda pendiente, no estaba en el alcance original de esta lista de tareas.
- [x] 4.2 Helper que crea `LinkRastreable` + `PublicacionExterna` (`modo = MANUAL`, `estado = ENVIADA`) en una sola operación y usa la clave de idempotencia para impedir duplicados — verificado con script end-to-end contra la base real: segundo intento sobre el mismo incidente devuelve `ok:false` con quién/cuándo se compartió, y solo queda 1 fila en `PublicacionExterna`
- [x] 4.3 Botón "Compartir" en el detalle de incidente/requerimiento/mascota, gateado con `requireRoleSession(GESTORES_INCIDENTES | GESTORES_REQUERIMIENTOS | GESTORES_MASCOTAS)` según corresponda — implementado (`CompartirBoton` + `POST /api/notificaciones-externas/compartir` + condicional `puedeGestionar` en las 3 páginas de detalle). Verificado: request sin sesión al endpoint da 403; `tsc`/`eslint` limpios sobre los archivos nuevos y modificados. **No verificado visualmente con un login real de cada rol — no tengo credenciales de prueba y no quise resetear la contraseña de un usuario existente. Falta como QA manual.**
- [x] 4.4 Al activar el botón, abrir `wa.me` con el mensaje pre-cargado y el link ya generado — implementado en `CompartirBoton.tsx`, mismo patrón que `InvitarVecino.tsx` (Web Share nativo con fallback a `wa.me`). **Verificación manual en mobile/desktop pendiente** (agrupada con las demás tareas manuales).
- [x] 4.5 Mostrar en el detalle si el evento ya fue compartido (quién y cuándo) en vez de ofrecer compartir de nuevo — implementado vía `obtenerCompartido()` + prop `yaCompartidoInicial`; lógica de datos verificada end-to-end en la tarea 4.2 (mismo query)
- [x] 4.6 Push a los gestores del tipo correspondiente cuando se carga un evento nuevo, con acceso directo a la acción de compartir (reutilizar `src/lib/push/enviarPush.ts`) — **ya cubierto por código existente, sin cambios:** las tres rutas de creación (`incidentes`, `requerimientos`, `mascotas`) ya hacen `enviarPushBroadcast(...)` a todos los suscritos (menos el autor) con `url` apuntando al detalle del evento — el mismo detalle donde ahora vive el botón "Compartir" (tarea 4.3). Agregar un push aparte solo para gestores duplicaría la notificación para ellos; se descarta a propósito.

## 5. Flujo de autenticación desde link externo

- [x] 5.1 Pantalla intermedia para usuario sin sesión que llega desde `/r/[codigo]`: explica la app y ofrece registrarse/iniciar sesión, preservando `callbackUrl` — implementado como banner condicional en `/login` y `/registro` (no una pantalla nueva separada, ver hallazgo de la tarea 1.3: el login ya no era "pelado", solo le faltaba leer `callbackUrl`). Corregido el bug real encontrado en 1.3: `LoginPage.handleSubmit` ahora respeta `callbackUrl` en vez de mandar siempre a `/inicio`, y el link "Registrate aquí"/"Iniciá sesión" lo propaga entre las dos páginas y en el diálogo de éxito del registro. Verificado con `curl`: el banner aparece solo con `callbackUrl` presente, en ambas páginas. **Recorrido completo con clic real desde WhatsApp sigue pendiente de verificación manual.**
- [x] 5.2 Ajustar el flujo de registro para marcar `Usuario.origenRegistro = WHATSAPP` cuando el registro llega con `callbackUrl` originado en un link externo — implementado en `POST /api/usuarios` leyendo la cookie `COOKIE_ORIGEN_WHATSAPP` (mismo patrón que `COOKIE_INVITACION`). Verificado end-to-end con `curl`: visitar `/r/[codigo]/ir` deja la cookie, y un registro posterior con esa cookie queda con `origenRegistro: "WHATSAPP"` en la base; el default sigue siendo `DIRECTO` para el resto (confirmado en la migración inicial, tarea 2.1)
- [ ] 5.3 Si la tarea 1.2 confirma que las rutas de detalle exigen `verificado`, construir la pantalla de "alta pendiente de aprobación" que retiene el destino original hasta que se apruebe la cuenta — omitir esta tarea si 1.2 concluye que no hace falta
- [ ] 5.4 Verificación manual end-to-end de los tres caminos del flujo de auth (sin cuenta, con cuenta sin sesión, con sesión activa) con un celular real, siguiendo `docs/proposal-whatsapp-bridge.md` §6

## 6. Métricas y línea de base

- [x] 6.1 Sección en el panel de admin con CTR por tipo de evento, clicks sin sesión, conversión click→registro, y evolución de usuarios activos 7d/30d — implementado en `src/app/(dashboard)/admin/notificaciones-externas/page.tsx` (ADMIN-only, agregado a "Accesos rápidos" del panel). Verificado con datos de prueba insertados a mano: `groupBy` por tipo y el join de clicks devuelven los números esperados
- [ ] 6.2 Medir y documentar la línea de base (eventos creados por semana, altas por semana, estimación manual de reportes que hoy solo están en WhatsApp) durante 2 semanas antes de habilitar el botón de compartir para todos los gestores — entregable: registro de línea de base documentado, no código. **No es una tarea de código — queda para el dueño de producto, con la ayuda de un admin del grupo de WhatsApp (D6 ya resuelta).**
